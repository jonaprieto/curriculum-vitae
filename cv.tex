\documentclass[$fontsize$]{cv}

% Packages
\usepackage{geometry}
\usepackage{polyglossia}
\usepackage{parskip}
\usepackage{hyperref}
\usepackage{libertine}
\usepackage{microtype}

% Document settings
\geometry{margin=$margin$}
\setmainlanguage{$lang$}
\definecolor{boxcolor}{HTML}{$boxcolor$}
\newcommand\boxheight{$boxheight$}

\begin{document}
{\Large \textbf{$name$}}\\[0.5em]
\header{$phone.primary$$if(phone.secondary)$\,\textbullet\,$phone.secondary$$endif$}
       {\href{mailto:$email$}{$email$}}
       {$address.line1$}
       {$address.line2$}

$if(intro)$
  $intro$\\[0.5em]
$endif$





  $if(experience)$
    \colorbox{Experience}
    $for(experience)$
      \listing{$experience.job$}{$experience.years$}{$experience.employer$, $experience.city$}{}
      $if(experience.tasks)$
        \begin{itemize}
          $for(experience.tasks)$
            $if(experience.tasks.years)$
              \item [$experience.tasks.years$] $experience.tasks.name$
            $else$
              \item $experience.tasks$
            $endif$
          $endfor$
        \end{itemize}
      $endif$
    $endfor$
  $endif$

  $if(education)$
    \colorbox{Education}
    $for(education)$
      \listing{$education.subject$}{$education.years$}{$education.institute$, $education.city$}{}
    $endfor$
  $endif$

$if(skills)$
  \colorbox{Skills}
  \begin{itemize}
    $for(skills)$
      \item $skills$
    $endfor$
  \end{itemize}
$endif$

$if(tech)$
  \colorbox{Technical Skills}
    $for(tech)$
     \texttt{$tech.lang$} [{$tech.year$}]\\
     {\textbf{Domain}:$tech.domain$}\\
      $if(tech.projects)$\\
      Software:
        \begin{itemize}
        $for(tech.projects)$
          \item [$tech.projects.year$] \href{$tech.projects.url$}{\textit{$tech.projects.name$}: $tech.projects.description$
        $endfor$
        \end{itemize}
      $endif$
    $endfor$
$endif$

\end{document}
