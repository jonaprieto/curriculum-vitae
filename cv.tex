\documentclass[$fontsize$]{cv}

% Packages
\usepackage{geometry}
\usepackage{polyglossia}
\usepackage{parskip}
\usepackage{hyperref}
\usepackage{libertine}
\usepackage{microtype}

% Document settings
\geometry{margin=$margin$}
\setmainlanguage{$lang$}
\definecolor{boxcolor}{HTML}{$boxcolor$}
\newcommand\boxheight{$boxheight$}

\begin{document}
{\Large \textbf{$name$}}\\[0.5em]
\header{$phone.primary$$if(phone.secondary)$\,\textbullet\,$phone.secondary$$endif$}
       {\href{mailto:$email$}{$email$}}
       {$address.line1$}
       {$address.line2$}
\href{$website.primary$}{\url{$website.primary$}}
$if(website.secondary)$\\
\href{$website.secondary$}{\url{$website.secondary$}}$endif$
\\[0.3em]

$if(intro)$
  $intro$\\[0.5em]
$endif$

$if(research)$
  \colorbox{Research Experience}
  $for(research)$
    \listing{$research.job$}{$research.years$}{$research.employer$, $research.city$}{}
    $if(research.tasks)$
      \begin{itemize}
        $for(research.tasks)$
          $if(research.tasks.years)$
            \item $research.tasks.name$ \hfill [$research.tasks.years$]
          $else$
            \item $research.tasks$
          $endif$
        $endfor$
      \end{itemize}
    $endif$
  $endfor$
$endif$

$if(lecturer)$
  \colorbox{Lecture Experience}
  $for(lecturer)$
    \listing{$lecturer.job$}{$lecturer.years$}{$lecturer.employer$, $lecturer.city$}{}
    $if(lecturer.tasks)$
      \begin{itemize}
        $for(lecturer.tasks)$
          $if(lecturer.tasks.years)$
            \item $lecturer.tasks.name$ \hfill [$lecturer.tasks.years$]
          $else$
            \item $lecturer.tasks$
          $endif$
        $endfor$
      \end{itemize}
    $endif$
  $endfor$
$endif$

  $if(education)$
    \colorbox{Education}
    $for(education)$
      \listing{$education.subject$}{$education.years$}{$education.institute$, $education.city$}{}
      $if(education.tesis-title)$
        \begin{itemize}\item \emph{$education.tesis-title$} with advisory of $education.tesis-advisor$. \end{itemize}
      $endif$
    $endfor$
  $endif$

$if(languages)$
  \colorbox{Languages}
  \begin{itemize}
    $for(languages)$
      \item $languages$
    $endfor$
  \end{itemize}
$endif$

$if(skills)$
  \colorbox{Skills}
  \begin{itemize}
    $for(skills)$
      \item $skills$
    $endfor$
  \end{itemize}
$endif$

$if(achievements)$
  \colorbox{Achievements}
  \begin{itemize}
    $for(achievements)$
      \item $achievements$
    $endfor$
  \end{itemize}
$endif$

\vspace*{3mm}

$if(tech)$
  \colorbox{Technical Skills}
    $for(tech)$
     $tech.lang$ [$tech.year$] Domain: $tech.domain$\\
      $if(tech.projects)$
      Software:
        \begin{itemize}
        $for(tech.projects)$
          \item
        \href{$tech.projects.url$}{\textit{$tech.projects.name$}}: $tech.projects.description$
        \hfill [$tech.projects.year$]
        $endfor$
        \end{itemize}
      $endif$
    $endfor$
$endif$

$if(references)$
  \colorbox{References}
  \begin{itemize}
    $for(references)$
      \item $references$
    $endfor$
  \end{itemize}
$endif$

\end{document}
